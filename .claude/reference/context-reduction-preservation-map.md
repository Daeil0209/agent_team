---
doc-type: reference-cache
PRIMARY-OWNER: team-lead
captured: 2026-04-29
LOAD-POLICY: on-demand reference only
topology-updated: 2026-04-29
---
# Context Reduction Preservation Map

## Purpose
This file preserves source-to-destination traceability for the 2026-04-29 compression of always-loaded governance surfaces.
It is a reference cache, not a skill, not a runtime state file, and not always-loaded context.

## Consumer And Ownership
- Primary consumer: `team-lead`.
- Use only for context-reduction preservation audits, user challenges about compression safety, duplicate-overlap checks, and source-to-destination traceability reviews.
- `CLAUDE.md` indexes the obligation to preserve traceability; it does not absorb this file as always-on doctrine.
- Lane agents do not load this file by default. They receive only the specific traceability facts packetized by `team-lead` when needed.

Compression is lawful only when the short surface remains stronger or equally strong on action, owner, trigger, stop condition, evidence burden, verification gate, acceptance truth, and live cross-reference integrity.

## CLAUDE.md Compression Map
- Core governance force, procedure-over-habit, evidence-over-assumption, user-surface proof, channel restraint, parallel-fit, quality-flow, preservation, design-intent, hardening, destructive-action limits, and hook-last rules remain in `CLAUDE.md` `## Core Laws`.
- Rule priority, top-level lane ownership, direct lead-local limits, and owner-surface separation remain in `CLAUDE.md` `### Ownership`.
- Boot, session start, monitoring, closeout, update/upgrade, self-growth, and sequence arbitration remain indexed in `CLAUDE.md` `## Control Sequences`; detailed boot/recovery lives in `.claude/skills/session-boot/SKILL.md` and `.claude/skills/session-boot/references/runtime-state-detail.md`; teardown detail lives in `.claude/skills/session-closeout/SKILL.md` and `.claude/skills/session-closeout/references/closeout-state-detail.md`.
- Channel names, correction reopening, one-owner-at-a-time flow, workflow/sequence precedence, and required stage ordering remain in `CLAUDE.md` `### Channel Law`; detailed planning fields live in `.claude/skills/work-planning/SKILL.md`; verification gates live in `.claude/skills/self-verification/SKILL.md`.
- Skill lazy-loading, lane-core priority, packet `REQUIRED-SKILLS`, frontmatter non-default loading, and teammate context isolation remain in `CLAUDE.md` `## Skill Loading`; lane-local details live in each `.claude/agents/*.md` role file and matching lane-core skill.
- Runtime truth, dispatch/receipt/progress/completion distinction, agent objections, tool-surface discipline, and handoff sufficiency remain in `CLAUDE.md` `### Communication And Reporting Law`; exact message classes, packet fields, lifecycle proof, and completion spine live in `.claude/skills/task-execution/references/message-classes.md`, `.claude/skills/task-execution/references/request-bound-fields.md`, `.claude/skills/task-execution/references/truth-rules.md`, and `.claude/skills/task-execution/references/completion-handoff.md`.
- Role separation, validator-only final PASS/HOLD/FAIL, low-risk close exceptions, user-surface acceptance, and test/validation surface freeze remain in `CLAUDE.md` `### Role And Acceptance Law`; carve-out term definitions live in `.claude/skills/validator/references/validator-lane-detail.md` `Acceptance Carve-Out Definitions`; lane execution detail remains in reviewer/tester/validator role files and lane-core skills.
- Meaning preservation, live-reference preservation, owner-boundary protection, update/upgrade route, compression blocker rule, and duplicate-overlap repair remain in `CLAUDE.md` `### Change And Preservation Law`; defect recurrence barriers live in `.claude/skills/self-growth-sequence/SKILL.md`.
- Safety, Claude Code prose-vs-technical enforcement boundary, hook-vs-settings enforcement split, runtime-state ownership, continuity-snapshot boundaries, runtime-cost claim limits, and owner-local detail placement remain in `CLAUDE.md` `## Conditional Rules`; official Claude Code evidence is cached in `.claude/reference/official-claude-code-reference.md`, while hook footprint and continuity-cost detail lives in `.claude/reference/runtime-footprint-and-continuity.md`.

## Team-Lead.md Compression Map
- The required pre-action gate remains in `.claude/agents/team-lead.md` `Priority 0`; expanded request-fit freezing lives in `.claude/skills/work-planning/SKILL.md`.
- Team-lead identity, configured-lane preference, lead-local exception handling, and binding stack remain in `Priority 1`; delegated lane contracts remain in `.claude/agents/researcher.md`, `developer.md`, `reviewer.md`, `tester.md`, and `validator.md`.
- Instruction interpretation and route opening remain in `RPA-1` and `RPA-2`; detailed work-planning consumption and edge-case reading order live in `.claude/skills/team-lead/SKILL.md`.
- Session boot, runtime-entry, monitoring, recovery, and closeout bridging remain in `RPA-3`; detailed runtime limits and lifecycle interpretation live in `.claude/skills/session-boot/*` and `.claude/skills/session-closeout/SKILL.md`.
- Work-planning field consumption, lead-local limits, no-implied-remainder stop rule, teammate context isolation, and packet-bound fact transfer remain in `RPA-4`; read-only audit default is a same-outcome scenario rule in this map, and lazy lane-core loading remains in `CLAUDE.md` `Skill Loading` plus lane role contracts. Exact packet translation lives in `.claude/skills/task-execution/SKILL.md` and `.claude/skills/task-execution/references/assignment-packet.md`.
- Communication truth, dispatch state, receipt, progress, completion, assignment/phase/lifecycle separation, lifecycle control, and assignment packet message classes remain in `RPA-5`; exact truth taxonomy lives in `.claude/skills/task-execution/references/message-classes.md`, `.claude/skills/task-execution/references/truth-rules.md`, and `.claude/skills/task-execution/references/lifecycle-control.md`.
- Agent objection handling remains in `RPA-6`; packet correction versus route replan criteria live in `.claude/skills/task-execution/references/scope-pressure.md`, `.claude/skills/task-execution/references/request-bound-fields.md`, and `.claude/skills/team-lead/SKILL.md`.
- Completion synthesis, open-surface rejection, sibling-output reconciliation, redispatch proof, and artifact integrity remain in `RPA-7`; detailed synthesis and reporting live in `.claude/skills/team-lead/SKILL.md` and `.claude/skills/self-verification/SKILL.md`.
- SV-PLAN and SV-RESULT ownership remains in `RPA-8`; full challenge lenses and output formats live in `.claude/skills/self-verification/SKILL.md`.
- Output truth and user-facing reporting remain in `RPA-9` and `RPA-10`; detailed communication expansions live in `.claude/skills/team-lead/SKILL.md`.
- Self-growth, interrupt/resume, and autonomous judgment remain in `RPA-11`, `RPA-12`, and `RPA-13`; detailed recurrence-barrier handling lives in `.claude/skills/self-growth-sequence/SKILL.md` and edge-case handling in `.claude/skills/team-lead/SKILL.md`.

## Agent Role Spine Migration Map
The 2026-04-29 lane role optimization keeps role files as always-loaded spines and moves field catalogs, specialist matrices, control detail, tool/proof detail, and handoff field detail to direct lane references. This is a source-to-destination preservation map, not a runtime loading rule.

Common preservation:
- Each `.claude/agents/<lane>.md` still owns lane identity, non-negotiable boundary, lazy lane-core loading trigger, packet authority, lane-local `SV-PLAN` classification, `execute` / `reconstruct-with-inference` / `scope-pressure` / `hold|blocker` stop states, control-only message handling, and lane-local completion boundary.
- Each `.claude/skills/<lane>/SKILL.md` now consumes `.claude/skills/<lane>/references/<lane>-lane-detail.md` as the detailed contract and states that `agents/<lane>.md` is the role spine, not the packet-field catalog.
- Each lane detail reference now contains `Role-Spine Handoff` and `Control Packet Discipline` so moved detail remains reachable without making it always-loaded context.
- Control-only messages remain safe without lane-core skill loading because each role file still references `message-classes.md`, `truth-rules.md`, `scope-pressure.md`, `phase-transition-control.md`, and `lifecycle-control.md`, and states that phase/lifecycle control is control-only unless an assignment-grade packet is primary.

Lane destinations:
- Developer packet fields, production basis classification, SKILL-AUTH, specialist matrix, durability/regression guard, and developer handoff detail live in `.claude/skills/developer/references/developer-lane-detail.md`; `.claude/agents/developer.md` keeps production-only boundary, design-intent preservation, first lawful execution path, blocker/pressure trigger, and no proof/review/validation/acceptance ownership.
- Researcher packet fields, benchmark fields, rendered/multi-modal evidence requirements, operational-reality classification, and researcher handoff detail live in `.claude/skills/researcher/references/researcher-lane-detail.md`; `.claude/agents/researcher.md` keeps evidence-only boundary, facts/inferences/assumptions separation, no mutation, no proof/validation/acceptance ownership, and rendered/external/benchmark trigger anchors.
- Reviewer packet fields, review lenses, severity mapping, specialist order, rendered/evidence gap handling, and validator-ready handoff detail live in `.claude/skills/reviewer/references/reviewer-lane-detail.md`; `.claude/agents/reviewer.md` keeps review-only boundary, findings-require-evidence law, plan/design review authority without route ownership, no implementation/proof/validation/acceptance ownership, and rendered-review anchor.
- Tester packet floor, UI intent proof matrix, defect-detection amplifiers, tool-tier detail, interaction coverage, human-facing checklist, specialist rule, and tester handoff detail live in `.claude/skills/tester/references/tester-lane-detail.md`; `.claude/agents/tester.md` keeps proof-only boundary, execution-evidence requirement, design-intent exercise rule, user-action/visible-result proof anchor, and no final acceptance ownership.
- Validator packet fields, verdict lenses, PASS prohibition detail, acceptance reconciliation, operator-runtime/rendered-quality protocols, acceptance carve-out definitions, and validator handoff detail live in `.claude/skills/validator/references/validator-lane-detail.md`; `.claude/agents/validator.md` keeps final-verdict boundary, weak-evidence PASS prohibition, design-intent PASS prohibition, decisive user-surface evidence rule, and no production/review/proof/remediation/orchestration ownership.

This migration is converged only if the same lane assignment still reaches the same stop state, proof burden, owner boundary, and completion truth as before compression.

## Team-Session-Sequences Optimization Map
- `.claude/skills/team-session-sequences/SKILL.md` keeps the session-sequence index and lead-side dispatch-detail spine: mandatory triggers, owner boundaries, stop rules, and the rule that oversized or mixed dispatch must be decomposed before sending. Runtime spine stays with `.claude/skills/session-boot/SKILL.md`, closeout spine stays with `.claude/skills/session-closeout/SKILL.md`, and packet schema stays with `.claude/skills/task-execution/SKILL.md`.
- `.claude/skills/team-session-sequences/references/*.md` owns the detailed lookup content moved out of the active spine: pre-dispatch self-verification gate detail, task decomposition detail, dispatch packet compliance controlled-value and presence-field tables, agent load guard sizing, dispatch execution contract detail, parallel shard/merge detail, and consequential upward handoff field catalog.
- `.claude/skills/task-execution/references/lane-additions.md` points to those `team-session-sequences/references/*.md` lookups for lane-specific packet discipline while retaining cross-lane base schema, common message classes, field format, lifecycle proof, and packet preflight ownership.
- This split is a context-cost optimization only. It does not delete procedure, relax dispatch gates, weaken upward handoff requirements, or make packet discipline optional.

## Skill Wrapper Optimization Map
- `.claude/skills/update-upgrade-sequence/SKILL.md` remains the directly routable `ACTIVE-SEQUENCE: update-upgrade-sequence` spine for `.claude` asset updates. Detailed update checks, skill introduction gates, and specialist routing rules moved to `.claude/skills/update-upgrade-sequence/references/*.md`.
- The former top-level `incident-response` skill name is retired. Binding incident detail lives in `.claude/skills/dev-workflow/references/incident-response.md`; callers must route incident work through `dev-workflow` rather than packetizing `incident-response` as a separate skill.
- The former top-level `gap-detect` skill name is retired. Binding YAGNI, gap detection, coverage scoring, gap-state packet, and iteration contribution detail lives in `.claude/skills/dev-workflow/references/gap-iteration.md`; benchmark feedback loops must point to that reference rather than a separate skill.
- This split preserves meaning while removing duplicate skill discovery surfaces. Only directly routable sequences or standalone reusable procedures remain top-level skills; absorbed workflow details live under the owning workflow's `references/` folder.

## Preservation Audit Rule
Before or after any high-traffic context reduction, verify:
1. every old cross-reference anchor still exists or every caller has been updated;
2. every removed rule has a named owner destination or is explicitly removed as duplicate under `[GOV-MIN]`;
3. shortened wording remains imperative and testable, not descriptive only;
4. detailed procedure is reachable by trigger, not always-loaded by habit;
5. skill and lane ownership remain lazy-loaded and packet-driven;
6. duplicate restatements are protected local anchors, not competing procedures; any divergence is resolved by naming the precise owner and tightening, re-homing, or removing the weaker duplicate;
7. the compressed surface does not claim hard technical enforcement where Claude Code official docs only support context/procedure influence.

If a destination cannot be named, restore the rule to the source surface or create the correct owner-local destination before landing the compression.

## Same-Outcome Scenario Matrix
Use this matrix to test whether compressed doctrine still produces the same procedural result as the longer source surface.

| Scenario | Required outcome after compression |
| --- | --- |
| Fresh consequential request | `Priority 0` classifies; `session-boot` opens first when RPA-3 triggers; otherwise `work-planning -> SV-PLAN` before execution, dispatch, or report. |
| Light answer-only request | `Priority 0` runs; no lane-core skill, workflow, dispatch, or governance detail loads by habit. |
| Read-only diagnostic, audit, or analytical work | Lead-local is default only under direct-work limits; team fan-out requires frozen context-cost or independent-evidence basis. |
| Lead-local consequential work | Allowed only with complete local work list, required skills, first action, `SV-PLAN`, no lane-owned/independent/proof/validator/destructive/executable/stronger-claim substitution, and no implied remainder. |
| Team-routed configured lane work | `task-execution` translates the frozen route into self-contained packets; `TeamCreate` precedes team-scoped `Agent`/assignment-grade `SendMessage`; standalone `Agent` is not configured lane dispatch. |
| Parallel-fit work | Independent bounded surfaces are marked parallel-fit; serial collapse needs a named cost/dependency basis; one blocked surface does not stall unrelated lawful independent surfaces. |
| Agent `hold|blocker` or `scope-pressure` | Consume explicit fields; classify as packet correction, route replan, parallel continue, or user-facing blocker; never smooth into progress, completion, or agent reluctance. |
| Packet correction | Lawful only inside same frozen owner, phase, deliverable, proof/acceptance chain, staffing shape, agent boundary, parallel grouping, and required verification gate. |
| Completion synthesis | Only completion-grade `MESSAGE-CLASS: handoff` or `MESSAGE-CLASS: completion` plus Common Completion Result Spine can synthesize; receipt/control/status/blocker/pressure cannot close work. |
| Acceptance carve-out | Reviewer+tester closure without validator remains unavailable for executable, destructive, security-sensitive, externally committed, or unresolved disagreement paths; definitions live in validator reference; ambiguity selects validator or `HOLD`. |
| Runtime/lifecycle truth | Dispatch, receipt, agent-start, progress, completion, lifecycle request, lifecycle control, cleanup, and acceptance remain distinct; `REQUESTED-LIFECYCLE` never clears debt by itself. |
| `session-state.md` continuity | Treat as generated startup anchor only; stale or auto-captured content never becomes authoritative runtime, task, cleanup, or acceptance state. |
| Hook/runtime cost claim | Separate official legality from local `[HOOK-LAST]` philosophy; measured claims need runtime evidence, otherwise report `design-level` or `UNVERIFIED`. |
| Context reduction or migration | Live reference check, duplicate-overlap audit, source-to-destination map, and imperative-force check are required before closure. |
| Duplicate doctrine overlap | Compact rule is trigger/anchor; precise owner surface controls detailed procedure; divergent duplicate is tightened, re-homed, or removed, not averaged. |
| User compliance challenge | Current conclusion becomes unverified draft; re-examine against philosophy, evidence, net benefit, owner fit, and preservation map; confirmed defect enters the narrowest owner hardening path. |

If any scenario produces a weaker action, weaker stop condition, weaker proof burden, weaker owner boundary, or weaker acceptance result than the longer source surface, the compression is not converged.
